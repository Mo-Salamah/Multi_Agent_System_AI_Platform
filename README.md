# Multi Agent System AI Platform

A demo showcase of a multi-agent AI platform for managing workstreams, events, and organizational workflows.

## Deploy

This site is deployed via GitHub Pages. Push to `main` to trigger a deployment.

## Local Development

```bash
pnpm install
pnpm dev
```

Open http://localhost:3000
